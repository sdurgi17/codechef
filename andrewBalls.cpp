#include <iostream>
#include <vector>
#include <cstdio>
#include <climits>
#include <algorithm>
#define MOD 1000000009

using namespace std;
int no_of_ways(int arr[], int n) {
	
}

int main(int argc, char const *argv[])
{
	int t;
	cin >> t;

	for (int i = 0; i < t; ++i) {
		int n;
		cin >> n;
		int arr[n];

		for (int j = 0; j< count; ++j) {
			cin >> arr[j];	
		}

		cout << no_of_ways(arr, n) << endl;
	}
	return 0;
}